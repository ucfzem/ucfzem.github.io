# Session 2026-06-11: AI Image Enhancer Migration & API Fixes

## Overview
Migrated AI Image Enhancer from Vercel to Cloudflare Pages + GitHub Pages.
Fixed API proxy worker with Replicate integration. Full conversation backup.

## Deployment URLs
- **GitHub Pages (frontend):** https://ucfzem.github.io/ai-image-enhancer/
- **Cloudflare Pages (frontend, alt):** https://ai-image-enhancer-afa.pages.dev
- **API Worker:** https://ai-image-enhancer-api.azer-tyu199p.workers.dev
- **Vercel (old, still running):** https://ai-image-enhancer-nine-ruby.vercel.app

## Architecture
Browser → GitHub Pages (static HTML/JS/CSS) → Cloudflare Worker (API proxy) → Replicate API

### File Locations in Repo
- **Static site:** `/ai-image-enhancer/` (Next.js static export output)
- **API Worker source:** `/ai-image-enhancer-deploy-api/worker.js`
- **Next.js source:** `/AI-Image-Enhancer/`

## API Keys (Cloudflare Worker Secrets)
| Variable | Status |
|---|---|
| `REPLICATE_API_TOKEN` | ✅ Working (free tier, rate limited) |
| `HUGGINGFACE_API_TOKEN` | ❌ Invalid/Expired |
| `DEEPAI_API_KEY` | Set but unused in worker |
| `CLOUDINARY_CLOUD_NAME` | Set but unused in worker |
| `CLOUDINARY_API_KEY` | Set but unused in worker |
| `CLOUDINARY_API_SECRET` | Set but unused in worker |

## Worker API Endpoint
- **URL:** `https://ai-image-enhancer-api.azer-tyu199p.workers.dev/api/process`
- **Method:** POST
- **Body:** FormData with `image` (file) and `tool` (string: restore|colorize|upscale|denoise|enhance)
- **CORS:** Enabled for all origins

### Replicate Models Used
| Tool | Model | Notes |
|---|---|---|
| restore | `tencentarc/gfpgan` | Face restoration |
| colorize | `tencentarc/gfpgan` | No colorize model available — uses GFPGAN fallback |
| upscale | `nightmareai/real-esrgan` | 4x upscaling |
| denoise | `tencentarc/gfpgan` | No denoise model — uses GFPGAN fallback |
| enhance | `tencentarc/gfpgan` | General enhancement |

## Worker Source Code
Located in repo at `/ai-image-enhancer-deploy-api/worker.js`. Full code:

```javascript
const VALID_TOOLS = ["restore", "colorize", "upscale", "denoise", "enhance"];

function arrayBufferToBase64(buffer) {
  let binary = "";
  const bytes = new Uint8Array(buffer);
  for (let i = 0; i < bytes.byteLength; i++) binary += String.fromCharCode(bytes[i]);
  return btoa(binary);
}

async function replicatePredict(token, modelVersion, input) {
  const resp = await fetch("https://api.replicate.com/v1/predictions", {
    method: "POST",
    headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" },
    body: JSON.stringify({ version: modelVersion, input }),
  });
  if (!resp.ok) {
    const body = await resp.text().catch(() => "");
    throw new Error(`Replicate ${resp.status}: ${body || resp.statusText}`);
  }
  const prediction = await resp.json();
  const getUrl = prediction.urls?.get;
  if (!getUrl) throw new Error("Replicate: no prediction URL");
  for (let i = 0; i < 30; i++) {
    await new Promise((r) => setTimeout(r, 2000));
    const poll = await fetch(getUrl, { headers: { Authorization: `Bearer ${token}` } });
    const status = await poll.json();
    if (status.status === "succeeded") return status.output?.[0] || status.output;
    if (status.status === "failed") throw new Error("Replicate: processing failed");
  }
  throw new Error("Replicate: timeout");
}

async function processWithReplicate(imageBuffer, fileType, tool, token) {
  const base64 = `data:${fileType || "image/png"};base64,${arrayBufferToBase64(imageBuffer)}`;
  const allTools = {
    restore: { version: "0fbacf7afc6c144e5be9767cff80f25aff23e52b0708f17e20f9879b2f21516c", input: { img: base64 } },
    colorize: { version: "0fbacf7afc6c144e5be9767cff80f25aff23e52b0708f17e20f9879b2f21516c", input: { img: base64 } },
    upscale: { version: "b3ef194191d13140337468c916c2c5b96dd0cb06dffc032a022a31807f6a5ea8", input: { image: base64, scale: 4 } },
    denoise: { version: "0fbacf7afc6c144e5be9767cff80f25aff23e52b0708f17e20f9879b2f21516c", input: { img: base64 } },
    enhance: { version: "0fbacf7afc6c144e5be9767cff80f25aff23e52b0708f17e20f9879b2f21516c", input: { img: base64 } },
  };
  const cfg = allTools[tool];
  return replicatePredict(token, cfg.version, cfg.input);
}

export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (request.method === "OPTIONS") {
      return new Response(null, {
        headers: {
          "Access-Control-Allow-Origin": "*",
          "Access-Control-Allow-Methods": "POST, OPTIONS",
          "Access-Control-Allow-Headers": "Content-Type",
        },
      });
    }
    if (url.pathname === "/api/process" && request.method === "POST") {
      try {
        const formData = await request.formData();
        const imageFile = formData.get("image");
        const tool = formData.get("tool");
        if (!imageFile || !tool) {
          return new Response(JSON.stringify({ error: "Image and tool are required" }), {
            status: 400,
            headers: { "Content-Type": "application/json", "Access-Control-Allow-Origin": "*" },
          });
        }
        if (!VALID_TOOLS.includes(tool)) {
          return new Response(JSON.stringify({ error: "Invalid tool" }), {
            status: 400,
            headers: { "Content-Type": "application/json", "Access-Control-Allow-Origin": "*" },
          });
        }
        if (!env.REPLICATE_API_TOKEN) {
          return new Response(JSON.stringify({ error: "API token not configured" }), {
            status: 500,
            headers: { "Content-Type": "application/json", "Access-Control-Allow-Origin": "*" },
          });
        }
        const imageBuffer = await imageFile.arrayBuffer();
        const imageUrl = await processWithReplicate(imageBuffer, imageFile.type, tool, env.REPLICATE_API_TOKEN);
        return new Response(JSON.stringify({ imageUrl, provider: "Replicate" }), {
          headers: { "Content-Type": "application/json", "Access-Control-Allow-Origin": "*" },
        });
      } catch (err) {
        return new Response(JSON.stringify({ error: err.message }), {
          status: 500,
          headers: { "Content-Type": "application/json", "Access-Control-Allow-Origin": "*" },
        });
      }
    }
    return new Response("Not Found", {
      status: 404,
      headers: { "Access-Control-Allow-Origin": "*" },
    });
  },
};
```

## Build Process
1. Removed `app/api/` (API routes not needed for static export)
2. Set `next.config.js` with `output: 'export'`, `basePath: '/ai-image-enhancer'`, `trailingSlash: true`
3. Set `api-client.ts` to point to worker URL
4. Built with `npm run build`
5. Copied `out/*` to repo `ai-image-enhancer/`

## Git
- **Repo:** `ucfzem/ucfzem.github.io`
- **Commit:** `6f1b17c` - "Add AI Image Enhancer static build with API proxy"
- **Branch:** main

## Vercel Deployment (unchanged)
- **URL:** https://ai-image-enhancer-nine-ruby.vercel.app
- **Inspect:** https://vercel.com/ucfzem-s-projects/ai-image-enhancer/21XeCZ3RabbgNGqpev72riThcY8Z

## Cloudflare Deployment
- **Pages project:** `ai-image-enhancer` (created via wrangler CLI)
- **Worker name:** `ai-image-enhancer-api`
- **Worker subdomain:** `ai-image-enhancer-api.azer-tyu199p.workers.dev`
- **wrangler version:** 4.97.0

## Known Issues
1. **colorize tool** uses GFPGAN (face restoration) — no proper colorization. Needs a dedicated colorization model/service.
2. **denoise tool** same — uses GFPGAN as fallback.
3. **HuggingFace tokens** all invalid — 4 different tokens attempted, all returned 401.
4. **Replicate free tier** rate limited to ~6 requests/minute.
5. **HuggingFace Inference API** endpoint changed from `api-inference.huggingface.co` to `router.huggingface.co/hf-inference` — but no valid token available.
6. **api-inference.huggingface.co** DNS doesn't resolve in current environment.

## Replicate API Notes
- Model versions looked up via Replicate API:
  - GFPGAN latest: `0fbacf7afc6c144e5be9767cff80f25aff23e52b0708f17e20f9879b2f21516c`
  - Real-ESRGAN latest: `b3ef194191d13140337468c916c2c5b96dd0cb06dffc032a022a31807f6a5ea8`
- GFPGAN input field: `img` (data URI)
- Real-ESRGAN input fields: `image` (data URI), `scale` (number)

## Original Vercel Env Vars
All 6 environment variables set on Vercel project `ai-image-enhancer` (production):
`REPLICATE_API_TOKEN`, `HUGGINGFACE_API_TOKEN`, `DEEPAI_API_KEY`,
`CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`
